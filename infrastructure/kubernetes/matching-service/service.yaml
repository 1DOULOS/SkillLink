apiVersion: v1
kind: Service
metadata:
  name: matching-service
  namespace: skilllink
  labels:
    app: matching-service
spec:
  selector:
    app: matching-service
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
