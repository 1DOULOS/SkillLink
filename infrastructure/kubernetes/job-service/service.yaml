apiVersion: v1
kind: Service
metadata:
  name: job-service
  namespace: skilllink
  labels:
    app: job-service
spec:
  selector:
    app: job-service
  ports:
    - name: http
      protocol: TCP
      port: 3003
      targetPort: 3003
  type: ClusterIP
